# --- Milestone 1: generator ---
numpy>=1.26
pandas>=2.2
pyarrow>=15.0
scikit-learn>=1.4        # roc_auc_score in the leak tests; LogisticRegression in the audit

# --- Milestone 2: EDA + leakage audit ---
matplotlib>=3.8          # the two EDA story figures

# --- Milestone 4: models + tracking ---
xgboost==3.3.0           # pinned: the model must be reproducible across machines/CI
mlflow>=3.0              # experiment tracking (local sqlite backend)
joblib>=1.3              # save/load the fitted model artifact

# --- Milestone 6: online scoring service ---
fastapi>=0.110           # the scoring API
uvicorn[standard]>=0.29  # ASGI server to run it
httpx>=0.27              # used by FastAPI's TestClient in the serving tests

# --- dev / CI ---
pytest>=8.0
ruff>=0.5
