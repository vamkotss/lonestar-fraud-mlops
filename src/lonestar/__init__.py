"""Lonestar Card Services fraud-detection MLOps package.

Modules are added milestone by milestone: the transaction generator (M1),
feature pipeline (M3), training (M4), scoring API (M6), and so on. This file
marks `lonestar` as an importable package and carries the version.
"""

__version__ = "0.1.0"
